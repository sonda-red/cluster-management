apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: intel
resources:
  - helm-release.yaml
configMapGenerator:
  - name: idpg-values
    files:
      - values.yaml
generatorOptions:
  disableNameSuffixHash: true
configurations:
  - kustomizeconfig.yaml