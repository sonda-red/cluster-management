apiVersion: resource.k8s.io/v1beta1
kind: ResourceClaimTemplate
metadata:
  name: intel-monitor-claim
  namespace: intel
spec:
  spec:
    devices:
      requests:
        - name: intel-monitor
          deviceClassName: gpu.intel.com
          adminAccess: true
          allocationMode: "All"        
